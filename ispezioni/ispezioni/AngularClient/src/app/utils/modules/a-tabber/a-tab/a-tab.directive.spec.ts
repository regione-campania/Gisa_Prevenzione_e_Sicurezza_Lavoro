import { ATabDirective } from './a-tab.directive';

describe('ATabDirective', () => {
  it('should create an instance', () => {
    const directive = new ATabDirective();
    expect(directive).toBeTruthy();
  });
});
