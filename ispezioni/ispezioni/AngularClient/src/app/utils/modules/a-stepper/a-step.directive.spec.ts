import { AStepDirective } from './a-step.directive';

describe('AStepDirective', () => {
  it('should create an instance', () => {
    const directive = new AStepDirective();
    expect(directive).toBeTruthy();
  });
});
